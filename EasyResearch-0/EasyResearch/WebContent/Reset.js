/**
 * 
 */

function chkreset()
{
	
	if((document.resetpassform.resetPassUsername.value=="") || (document.resetpassform.resetPassEmailId.value==""))
		alert("Enter username and Email");
	
	}